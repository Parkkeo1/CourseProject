from sklearn.decomposition import LatentDirichletAllocation

# TODO
# def calculate_lda_for_new_doc(tfidf_matrix, doc_index=-1, n_components=10):
#     lda = LatentDirichletAllocation(n_components=n_components, learning_method="online")
#     lda.fit(tf_idf_matrix)
#     lda_for_doc = lda.transform(tf_idf_matrix[doc_index]
#     return
